Files included:

switch_act.asm:		If Switch, act like a block
switch_hurt.asm:	If Switch, hurt the player
switch_kill.asm:	If Switch, kill the player
switch_shatter.asm:	If Switch, shatter

These conditional Switch blocks can be reversed using the !reversed define.